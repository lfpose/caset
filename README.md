# caset

A tiny static site at [caset.cl](https://caset.cl). Zero build: plain files in `public/`, served as Cloudflare Workers static assets.

```sh
npm install
npm run dev      # local preview
npm run deploy   # publish
```
